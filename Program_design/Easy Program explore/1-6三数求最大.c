#include<stdio.h>
int main()
{
    int x,y,z,max;
    scanf("%d %d %d",&x,&y,&z);
    if(x>y)
    max=x;
    else
    max=y;
    if(max>z);
    else
    {
      max=z;
    }
    printf("%d",max);
    


}